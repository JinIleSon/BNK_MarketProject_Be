# ===== common (local/prod 공통) =====
spring:
  application:
    name: NICHIYA
  profiles:
    active: prod

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: kr.co.bnk_marketproject_be.dto

  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8080
  servlet:
    context-path: /NICHIYA

# 어드민 로고 업로드 경로 (배포용 세현 수정)
app:
  upload:
    dir: upload

# ===== local =====
---
spring:
  config:
    activate.on-profile: local
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/xe
    username: NICHIYA
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update            # 로컬 개발은 편하게 update
    open-in-view: false
    properties:
      hibernate.format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.mybatis: info

# ===== prod (운영) =====
---
spring:
  config:
    activate.on-profile: prod
  datasource:
    url: jdbc:oracle:thin:@//43.200.69.114:1522/XEPDB1   # Service name 방식
    username: NICHIYA
    password: 1234         # 환경변수로 주입
  jpa:
    hibernate:
      ddl-auto: none              # ★ 스키마 올리기 전엔 none
    open-in-view: false
    properties:
      hibernate.format_sql: true

  # 구글 로그인 클라우드 콘솔 auth 설정
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope: email, profile
            # redirect-uri는 기본값이 {baseUrl}/login/oauth2/code/{registrationId}
            # 필요하면 아래처럼 명시 가능:
            #redirect-uri: "ec2-43-200-69-114.ap-northeast-2.compute.amazonaws.com/NICHIYA/login/oauth2/code/google"
            
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/kakao"
            scope: profile_nickname, account_email
            client-name: Kakao
            provider: kakao

          naver:
            client-id: ${NAVER_CLIENT_ID}
            client-secret: ${NAVER_CLIENT_SECRET}
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/naver"
            scope: name, email
            client-name: Naver
            provider: naver

        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
  mail:
    host: smtp.gmail.com
    port: 587
    username: machinemayou@gmail.com
    password: xpdgopkyeyxqotpz
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            required: true
            enable: ture
          ssl:
            trust: smtp.gmail.com

# 어드민 로고 업로드 경로 (배포용 세현 수정)
app:
  upload:
    dir: upload
  #base-url: http://43.200.69.114:8080/NICHIYA

mybatis:
  configuration:
    map-underscore-to-camel-case: false

